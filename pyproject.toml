[build-system]
requires = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "TE_CRE_network"
version = "0.0.1"
authors = [
  { name="Crystal Wen", email="mccwen@umich.edu" },
]
description = "TE_CRE_network package"
readme = "README.md"
requires-python = ">=3.8"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]

[tool.setuptools]
packages = ["TE_CRE_network"]
py-modules = ["steamer_enhancer"] # dash, not underscore
